services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: veyronis
      POSTGRES_HOST_AUTH_METHOD: trust
    ports:
      - "127.0.0.1:55432:5432"
    volumes:
      - veyronis-local-postgres:/var/lib/postgresql/data

volumes:
  veyronis-local-postgres:
